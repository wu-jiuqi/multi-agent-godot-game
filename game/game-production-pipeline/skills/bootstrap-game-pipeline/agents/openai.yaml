interface:
  display_name: "Bootstrap Game Pipeline"
  short_description: "Initialize a governed game-production workspace."
  default_prompt: "Use $bootstrap-game-pipeline to initialize this game project safely."
