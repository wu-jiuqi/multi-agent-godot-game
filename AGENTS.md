# 仓库协作规则

## 目标

本仓库用于沉淀可复用的 Codex Agent 和多 Agent 管线。所有设计必须能够说明输入、输出、验收方式、失败后的退回路径，以及必须由人类做出的判断。

## 工作原则

- 优先解决真实任务，再把重复流程抽象为 Agent、Skill、模板或校验脚本。
- 保持 Skill 的 `SKILL.md` 精简；详细规范放入 `references/`，确定性操作放入 `scripts/`，输出模板放入 `assets/`。
- 新建 Skill 时使用小写字母、数字和连字符命名，并执行对应验证脚本。
- 不在缺少输入或验收标准时擅自补全影响项目方向的决策。
- 修改共享契约时，检查所有依赖它的下游 Agent 和示例。

## Git 规则

- 按功能需求拆分提交。
- 提交信息使用中文，格式为 `feat: 提交信息`。
- 提交前检查变更范围和验证结果，避免覆盖无关改动。

